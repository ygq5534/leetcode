#include <iostream>
#include <vector>

using namespace std;

bool solution(vector<int> &list_a, int target){
	int start = 0, end = list_a.size(), mid;
	while(start <= end){
		mid = (start + end) / 2;
		if (mid == target){
			return true;
		}
		if (list_a[mid] < )
	}
}

int main(){
	vector<int> list_a = {2,5,6,0,0,1,2};

}
